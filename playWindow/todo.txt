main wave
- zoom out flashing to black
- silence not wide enough, subpixel wrong
- mark subpixel wrong
- check -ve clear with div

overview
- fix gaps
- lens dim and flashing

sizing window flashing, short and crashes

filelist broken

jpeg tag
 - westwing jpeg no SOI, should manage jpegImage deletion in fileThread

hls, shoutcast
- shoutcast
  - title display broken
- hls
  - channel change
  - bitrate change
  - go back in time
  - broadcast clocktime display

command line invocation
- web sites
- hls file list
- group of files etc,
- links to directories etc

direct2d
- investigate src coordinates to subpixel accuracy
- investigate bitmapbrush with alpha
  - gradient brush for waveform

play
- more buttons
- play on off
- stutter when stopped
- play region marking

editing
- mute marking
- save with edits
- compress to aaa

// timePoint snippets
auto timePointTz = date::make_zoned (date::current_zone(), mTimePoint);
mCurTimeStr = date::format (kTimeFormatStr, floor<chrono::seconds>(datePoint));

mStartDatePoint = date::floor<date::days>(mStartTimePoint);
const auto seconds = chrono::duration_cast<chrono::seconds>(mStartTimePoint - mStartDatePoint);

uint32_t startFrame = uint32_t((uint32_t(seconds.count()) - kStartTimeSecondsOffset) * kFramesPerSecond);
auto datePoint = floor<date::days>(timePoint);
auto timeOfDay = date::make_time (chrono::duration_cast<chrono::milliseconds>(timePoint - datePoint));
mCurDateStr = date::format (kTimeFormatStr, floor<chrono::seconds>(timePoint));
